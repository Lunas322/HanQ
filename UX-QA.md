# HanQ UX/UI QA 리포트

- 일시: 2026-08-25
- 브랜치: `develop` (`0f9c235`)
- 기준: `~/Desktop/other me` 볼트의 UX/UI 지식 27개 개념 페이지
- 심각도 등급: 같은 볼트 `wiki/concepts/UX-디자인/디자인-QA.md`의 4단계를 그대로 사용
- 짝 문서: [`QA.md`](./QA.md) (기능·보안·캐시 QA)

---

## 0. 기준을 어떻게 썼는가 — 먼저 읽을 것

볼트의 UX 지식은 **단일 소스 `UX-UI-Knowledge-Base`(신뢰도 C, AI 생성 확인됨)** 에서 왔다.
볼트 자신의 제1원칙("틀린 지식은 없는 지식보다 나쁘다")을 지키려면 **어느 기준이 1차 자료로 검증됐고
어느 것이 C등급 단독인지 구분해서 인용해야 한다.** 이 리포트는 그 구분을 항목마다 표시했다.

| 표시 | 의미 | 이 QA에서 쓴 기준 |
|---|---|---|
| **[A]** | 1차 자료 대조 완료 | WCAG 2.2 대비비(4.5:1 / 3:1) · SC 2.5.8 터치 24×24 AA + 예외 5가지 · WCAG 1.4.1 색 단독 금지 · 1.4.11 UI 컴포넌트 대비 · 한국 남성 색각이상 5.6% · 장애인차별금지법 제21조(**"법인"이 범주에 포함**) |
| **[C]** | AI 생성 소스 단독, 미검증 | 터치 44/48 실무 권장 · 타입 스케일 6~10단계 · 8pt 그리드 · 한글 행간 1.6 · Toast 3~5초 · 응답 시간 100/300ms |
| **[충돌]** | 소스 내부에서 값이 어긋남 | 응답 시간 임계값(400/300/1s/100ms **4번 충돌**) · 다국어 길이 증가율(30~40% vs 130~180%) |

**[C] 항목은 "고쳐야 한다"가 아니라 "왜 이렇게 했는지 설명할 수 있어야 한다"로 읽을 것.**
반대로 **[A] 항목은 근거가 확정된 지적**이므로 논쟁 대상이 아니다.

법적 판단 하나만 짚는다 — 볼트가 1차 자료로 확인한 바에 따르면 장애인차별금지법의 적용 범위는
**규모가 아니라 범주로 정해지고 "법인"이 그 범주에 포함**된다.
"개인 프로젝트라 접근성은 나중에"라는 판단이 **법인화 시점에 바로 깨진다.**

---

## 1. 요약

| 등급 | 건수 | 정의(볼트 기준) |
|---|---|---|
| **Critical** | **0** | 기능 사용 불가, 데이터 손실, 접근성 완전 차단 |
| **High** | **11** | 주요 기능 오작동, 심각한 시각 오류 → 배포 전 수정 |
| **Medium** | **12** | 부분적 불일치, 사용 가능하나 어색 → 다음 스프린트 |
| **Low** | **4** | 1~2px 차이 → 백로그 |

Critical이 0건인 것은 의미가 있다. **키보드만으로 핵심 플로우가 완주된다**(볼트가 "가장 저렴하고
효과적인 테스트"로 꼽은 것). 시맨틱 태그를 제대로 썼고 `aria-*`가 붙어 있어서, 접근성이
**차단된 곳은 없고 미달인 곳만 있다.** 이 차이는 크다.

가장 큰 덩어리는 하나다 — **색 토큰의 대비**. High 11건 중 4건이 여기서 나오고,
전부 `globals.css`의 토큰 4개를 고치면 한 번에 정리된다.

---

## 2. High — 배포 전 수정 (11건)

### H1. `--color-tertiary`가 본문 크기 텍스트 19개 파일에 쓰이는데 대비 미달 [A]

```
--color-tertiary: #8b95a1  on #ffffff  →  3.04:1
                             on #f2f4f6 (bg-muted) →  2.76:1
                             on #F9FAFB (bg-page)  →  2.91:1
```

**WCAG 1.4.3 AA는 4.5:1을 요구한다.** 큰 텍스트 예외(18pt≈24px 이상, 또는 14pt≈18.66px 이상 bold)를
쓸 수 있는 곳이 **한 군데도 없다** — `text-tertiary`가 붙은 모든 자리가 **11~14px**다.

실제 사용처(전수):

| 크기 | 위치 | 무엇 |
|---|---|---|
| 11px | `TranslatingBadge.tsx:8` | "번역 중" 배지 |
| 12px | `QuestionCard.tsx:44` · `AnswerItem.tsx:54` · `QuestionDetail.tsx:55` | 작성 시각 |
| 12px | `QuestionCard.tsx:50` | 좋아요·댓글 수 |
| 12px | `TextField.tsx:29` | 글자수 카운터 |
| 12px | `PollVote.tsx:72,142` | 투표 수, "다시 선택 가능" 안내 |
| 12px | `PollBuilder.tsx:48` | 투표 힌트 |
| 12px | `page.tsx:64` | 랜딩 약관 문구 |
| 12px | `notifications/page.tsx:61` | 알림 시각 |
| 13px | `ProfileSummary.tsx:35` · `LikeButton.tsx:49` · `PollBuilder.tsx:74` · `ProfileEditForm.tsx:119` | 프로필 보조정보, 좋아요 수, 삭제 버튼 |
| 13px | `page.tsx:46` | "인기 질문" 섹션 제목 |
| 14px | `home/page.tsx:52` · `detail/page.tsx:109` · `notifications/page.tsx:29` · `AuthoredQuestionList.tsx:27` | **빈 상태 문구 전부** |
| 14px | `error.tsx:30` | **에러 화면 설명 문구** |
| 14px | `Tab.tsx:22` · `BottomNavigation.tsx:35` · `DeleteMenu.tsx:107` | 비활성 탭, 비활성 내비 라벨, 삭제 확인 설명 |

**빈 상태와 에러 화면이 여기 걸린 게 가장 나쁘다** — 사용자가 막혔을 때 읽어야 하는 문구가
가장 안 읽히는 색이다.

수정 방향: 토큰 값을 **4.5:1을 넘는 회색으로 올린다.** 볼트가 예시로 든 경계값은 `#767676`(4.54:1),
`#6B7280`(4.83:1)이다. 현재 `--color-secondary: #6b7684`가 이미 4.62:1로 통과하므로,
**tertiary를 없애고 secondary로 흡수할지**가 실제 판단 지점이다.
그러면 위계가 3단계(ink / strong·body / secondary)로 줄어드는데,
볼트 기준으로는 오히려 맞다 — "한 화면에 3단계 이상의 텍스트 위계를 쓰지 않는다" [C].

### H2. 오류 메시지 색이 대비 미달 [A]

```
--color-like: #f04452  on #ffffff  →  3.71:1     (기준 4.5:1)
```

`role="alert"`가 붙은 **모든 오류 문구가 13px `text-like`** 다 —
`AskForm.tsx:66` · `AnswerInput.tsx:42` · `ProfileEditForm.tsx:139` ·
`DeleteMenu.tsx:112` · `GoogleLoginButton.tsx:42`.

같은 색이 **삭제 버튼 흰 텍스트 배경**(`DeleteMenu.tsx:134`, 15px bold)으로도 쓰인다.
`#fff` on `#f04452` = **3.71:1** — 15px bold는 큰 텍스트 기준(18.66px 이상 bold)에 못 미치므로
4.5:1이 필요하다. **미달.**

볼트가 "실무에서 가장 자주 발견되는 문제 Top 5"의 ①로 꼽은 항목이고,
"가장 중요한 텍스트가 가장 안 읽힌다"는 점에서 H1보다 우선순위가 높을 수도 있다.

### H3. 플레이스홀더 대비 1.40:1 [A]

```
--color-disabled: #d1d6db  on --color-page(#F9FAFB) → 1.40:1
```

`TextField.tsx:2` · `AnswerInput.tsx:58` · `PollBuilder.tsx:66` 세 곳 전부
`bg-page` 위에 `placeholder:text-disabled`다.

볼트의 대비 검증 목록은 **플레이스홀더를 4.5:1 대상으로 명시**한다 — WCAG 면제 대상은
`disabled` 상태이지 플레이스홀더가 아니기 때문이다.
1.40:1은 **거의 안 보이는 수준**이고, 볼트가 든 실패 사례(`#BBB` 1.9:1)보다 나쁘다.

### H4. 입력 필드·버튼 테두리 대비 1.23:1 [A]

```
--color-default: #E5E8EB  on #ffffff → 1.23:1     (WCAG 1.4.11 기준 3:1)
--color-muted:   #f2f4f6  on #ffffff → 1.10:1     (divide-muted 구분선)
```

**WCAG 1.4.11 (Non-text Contrast)는 UI 컴포넌트 경계에 3:1을 요구한다.**
`border-default`가 실제로 쓰이는 곳은 전부 **상태를 구분하는 경계**다:

- `Chip.tsx:4` — 카테고리 필터 **선택/미선택 구분**
- `LikeButton.tsx:49` — 좋아요 **누름/안누름 구분**
- `PollVote.tsx:107` — 투표 선택지 **선택/미선택 구분**
- `PollBuilder.tsx:86` — 선택지 추가 버튼

즉 **테두리가 정보를 전달하는데 그 테두리가 안 보인다.** 밝은 야외에서 특히 그렇다.

### H5. `word-break: keep-all`이 없다 [C, 단 근거가 강함]

```
$ grep -rn "keep-all\|word-break\|overflow-wrap" src
(결과 없음)
```

볼트의 타이포그래피 페이지는 한글 특수성 표에서 **`word-break: keep-all` 필수**를 굵게 표시하고,
UX 라이팅 페이지에서도 **"줄바꿈 — `keep-all` 필수, 줄 끝에 조사만 남지 않게"** 로 다시 강조한다.
한 소스에서 두 번 나오는 항목은 이것뿐이다.

없으면 어떻게 되나: 브라우저 기본값(`normal`)은 CJK를 **글자 단위로 끊는다.**
`QuestionCard`의 17px 제목, `TranslatableQuestion`의 22px 제목처럼 폭이 좁고 글자가 큰 곳에서
**"한국에서 자취할 때 보증인이 필/요한가요?"** 같은 어절 중간 줄바꿈이 나온다.

`overflow-wrap: break-word`도 없어서 **반대 문제도 있다** — 질문 본문에 긴 URL을 붙이면
컨테이너를 뚫는다. 두 속성은 짝으로 넣어야 한다.

`globals.css`의 `@theme` 아래 `body` 규칙 한 블록이면 끝난다.

### H6. `prefers-reduced-motion` 미대응 [A — WCAG 2.3.3 / 볼트가 1차 검증]

```
$ grep -rn "reduced-motion\|motion-safe\|motion-reduce" src
(결과 없음)
```

애니메이션은 한 곳이다 — `Skeleton.tsx:9`의 `animate-pulse`. 그런데 이게 **모든 화면에 깔린다**:
전 라우트의 `loading.tsx`, `QuestionListSkeleton`, `AnswerListSkeleton`, `ProfileSummarySkeleton`.
느린 회선에서 첫 화면 전체가 깜빡인다.

볼트의 모션 접근성 페이지는 이 절을 **"선택 사항이 아니다"** 로 시작하고,
전정기관 장애·편두통·광과민성 발작을 근거로 든다.
(유병률 수치는 볼트가 2026-08-25에 **"35%는 40세 이상 한정값"** 으로 정정했으니 그 숫자를 인용하지 말 것 —
결론은 유병률이 아니라 **영향받는 사람에게 실제 신체 증상이 생긴다**는 사실에서 나온다.)

볼트가 꼽은 **"방법 2 — 이동을 제거하고 페이드는 유지"** 가 여기에 딱 맞는다.
`animate-pulse`는 opacity 변화라 이동이 없으므로, **완전 제거가 아니라 주기를 늘리거나
정적 배경으로 대체**하는 선택지가 있다.

### H7. `viewport-fit=cover`를 켜놓고 safe-area 보정을 안 했다 [A — 플랫폼 규약]

`src/app/[lang]/layout.tsx:33`
```ts
viewport: { ..., viewportFit: "cover" }
```

```
$ grep -rn "safe-area\|env(" src
(결과 없음)
```

**이 조합이 문제다.** `viewport-fit=cover`는 "노치와 홈 인디케이터 영역까지 내가 직접 그리겠다"는
선언이다. 선언해놓고 `env(safe-area-inset-*)`를 안 쓰면 **하단 고정 요소가 홈 인디케이터에 깔린다.**
켜지 않았으면 브라우저가 알아서 피해줬을 텐데, 켰기 때문에 더 나빠졌다.

영향받는 요소 셋 — 전부 하단 고정이고 전부 핵심 액션이다:

| 위치 | 현재 하단 여백 | 홈 인디케이터(≈34px) |
|---|---|---|
| `BottomNavigation.tsx:56` — 홈/작성/마이 탭바 | `py-3` (12px) | **가려짐** |
| `page.tsx:62` — 랜딩 구글 로그인 CTA | `pb-5.5` (22px) | **일부 가려짐** |
| `AnswerInput.tsx:37` — 답변 입력창 | `pb-[14px]` | **가려짐** |

볼트가 반응형 페이지에서 예시 코드까지 준 항목이다:
`.bottom-nav { padding-bottom: env(safe-area-inset-bottom); }`

**랜딩 CTA가 가려진다는 게 특히 아프다** — 서비스 전체의 첫 전환 지점이다.

### H8. 성공 피드백이 전혀 없다 [C]

```
$ grep -rni "toast\|snackbar\|undo" src
(결과 없음)
```

볼트가 **"실무에서 가장 중요한 표"** 라고 이름 붙인 피드백 선택 기준표의 첫 줄이
`Toast / Snackbar — 방해 낮음 / 액션 불필요 / 작업 완료`다. HanQ에는 그 층이 통째로 없다.

무음으로 끝나는 동작:

| 동작 | 현재 | 사용자가 아는가 |
|---|---|---|
| 답변 등록 | 폼만 비워짐 (`AnswerInput.tsx:29`) | 목록에 나타나므로 **간접 확인 가능** |
| 프로필 저장 | `/my`로 리다이렉트 | **저장됐는지 불명확** |
| 좋아요 | 낙관적 UI만 | 시각 변화 있음 ✅ |
| 투표 | 낙관적 UI만 | 시각 변화 있음 ✅ |
| **답변 삭제** | **항목이 사라짐** | **성공인지 오류인지 구분 불가** |

가장 나쁜 건 삭제다. 볼트의 오류·빈 상태 체크리스트가 요구하는
**"접근성: `role="alert"`로 즉시 안내되는가"** 가 성공 경로에서 빠져 있다 —
스크린리더 사용자에게 **항목이 조용히 사라지는 것**과 **삭제에 성공한 것**은 구분되지 않는다.

투표 실패가 조용히 롤백되는 문제(`QA.md` 2-5)도 결국 이 층이 없어서 표면화된다.
볼트의 인터랙션 페이지가 "흔한 실수"로 명시한 **"낙관적 UI의 실패 처리를 안 한다 →
사용자는 성공한 줄 안다"** 가 그대로 재현되고 있다.

### H9. 모달에 focus trap과 포커스 복귀가 없다 [A — WAI-ARIA APG]

`DeleteMenu.tsx:93~141`

되어 있는 것: `role="dialog"` ✅ · `aria-modal="true"` ✅ · `aria-labelledby` ✅ ·
Escape 닫기 ✅ (`:28~40`) · 취소 버튼 `autoFocus` ✅

빠진 것 둘:

1. **Tab이 모달 밖으로 나간다.** 뒤 페이지의 링크·버튼으로 포커스가 새고,
   화면에는 모달만 보이는데 포커스는 안 보이는 곳에 있게 된다.
2. **닫을 때 트리거로 포커스가 안 돌아간다.** 볼트가 **"가장 자주 빠뜨린다"** 로 표시한 항목이다.
   취소하면 포커스가 `body`로 떨어져서 **처음부터 Tab을 다시 눌러야 한다.**

배경 스크롤 고정(scroll lock)도 없어서 모달 뒤 페이지가 스크롤된다.

볼트의 "포커스 이동이 필요한 8순간" 중 이 컴포넌트가 걸리는 건 **모달 열림·모달 닫힘·삭제 후** 셋이다.

### H10. 페이지 제목이 heading이 아니다 — 4개 화면에 `h1`이 없다 [A — WCAG 1.3.1]

`Header.tsx:29`
```tsx
<span className="min-w-0 truncate text-center font-bold">{title}</span>
```

헤더 제목이 `<span>`이라, 헤더에만 제목이 있는 화면은 **h1이 아예 없다.**

| 화면 | h1 | 출처 |
|---|---|---|
| 랜딩 `/ko` | ✅ | `HeroContent.tsx:9` |
| 상세 `/ko/detail/[id]` | ✅ | `TranslatableQuestion.tsx:26` |
| 마이 `/ko/my` · 유저 `/ko/users/[id]` | ✅ | `ProfileSummary.tsx:29` |
| **홈 `/ko/home`** | ❌ | — |
| **질문 작성 `/ko/ask`** | ❌ | 헤더 title이 `<span>` |
| **알림 `/ko/notifications`** | ❌ | 헤더 title이 `<span>` |
| **프로필 수정 `/ko/profile/edit`** | ❌ | 헤더 title이 `<span>` |

스크린리더 사용자는 **h 키로 제목을 순회해 현재 화면이 무엇인지 파악한다.**
로그인 후 가장 많이 머무는 홈에 h1이 없다.

볼트의 성능·SEO 페이지가 **"제목 구조가 시각적 위계와 일치하는가 ← 디자이너가 확인할 것"** 으로
콕 집은 항목이다 — h2처럼 보이는 것이 실제로 h2인지.

같은 뿌리의 문제 하나 더: **skip link가 없다.** 상세 페이지에서 키보드 사용자는
헤더를 매번 지나야 본문에 닿는다.

### H11. 제출 버튼이 왜 비활성인지 알려주지 않는다 [C, 근거는 명확]

`AskForm.tsx:28~33, 75`
```tsx
const canSubmit = title.trim() && content.trim() && categoryId !== null && ...;
<Button disabled={!canSubmit || isPending} ... />
```
`ProfileEditForm.tsx:148` 도 동일 (`name.trim().length === 0`).

볼트가 **"Disabled를 다시 생각한다"** 라는 별도 절을 할애한 지점이고,
꼽은 4가지 문제가 그대로 재현된다:

- 왜 비활성인지 알 수 없다 — 제목·본문·카테고리 셋 중 **무엇이 빠졌는지 표시가 없다**
- **스크린리더가 건너뛴다 — 버튼의 존재 자체를 모른다**
- 대비가 낮다 — `FILL_DISABLED`는 `bg-muted text-disabled` = **1.33:1**
- "고장났나?" 생각한다

카테고리를 안 고른 사용자는 **버튼이 왜 회색인지 모른 채 폼 상단으로 되돌아가 훑어야 한다.**

볼트의 대안 1번이 이 화면에 정확히 맞는다:
**활성으로 두고, 누르면 무엇이 부족한지 알려주고 해당 필드로 스크롤 + 포커스.**
서버 검증(`validateQuestionDraft`)이 이미 `CATEGORY_REQUIRED` 같은 코드를 반환하고
사전에 문구까지 있으므로, **추가로 만들 것이 거의 없다.**

---

## 3. Medium — 다음 스프린트 (12건)

| # | 항목 | 근거 | 내용 |
|---|---|---|---|
| M1 | **헤더 아이콘 버튼 20×20** | [A]+[C] | `Icon.tsx:87`의 `l` = 20px. 패딩이 없어 버튼 박스가 곧 아이콘 크기다. `NotificationBell` · `DeleteMenu` · `BackButton` · ask 레이아웃 닫기 버튼 전부 해당. **WCAG 2.2 SC 2.5.8 AA(24×24)에 미달**이지만 — 각 헤더 슬롯에 인접 타깃이 없어 **Spacing 예외로 기술적으로는 통과**한다. 다만 실무 권장 44/48 [C]에는 크게 못 미친다. 시각 크기는 두고 `::before { inset: -12px }` 로 히트 영역만 넓히는 것이 볼트의 해법. |
| M2 | 나머지 터치 타깃 | [C] | Chip 34px(`h-8.5`) · LikeButton sm ≈30px · md ≈36px. AA 24는 통과, 실무 44/48 미달. ✅ 잘된 것: Button md 44 · lg 56 · TextField 52 · 투표 선택지 48 · 모달 버튼 48 · FAB 52 |
| M3 | **타입 스케일이 없다** | [C] | `text-[11/12/13/14/15/16/17/18/20/22/25/34/38/40px]` **13단계**를 임의 px로 직접 씀. 볼트 기준은 **6~10단계 + 의미 기반 이름**(`body-l`, `caption`). 색은 `@theme` 토큰으로 정리해놓고 타이포만 토큰이 없다. 더불어 **px 고정이라 rem이 아니다** — 볼트 접근성 페이지의 "rem 사용, px 고정 지양"에 걸린다 |
| M4 | 8pt 그리드 이탈 | [C] | `mb-[14px]`×4 · `pt-[18px]`×3 · `px-[10px]`×3 · `mt-[10px]` · `pb-[114px]` · `gap-[5px]` · `py-[7px]` 등. 볼트의 디자인 QA 체크리스트 문구가 정확히 **"13px 같은 값이 없는가"** 다 |
| M5 | **일본어 문구 넘침** | [A — 실측] | `ProfileMenu.tsx:43` 컨테이너 `w-32`(128px) − `px-4`(32px) = **가용 96px**. `ja.ts`의 `editProfile`은 **プロフィール編集 8자 × 14px ≈ 112px** → 넘쳐서 2줄이 된다. `cancel`도 취소(2자) → キャンセル(**5자**). 볼트의 핸드오프 체크리스트 **"다국어에서 길이가 늘어나도 괜찮은가"** 미검증. 한↔일 양방향이 존재 이유인 서비스인데 **일본어로 화면을 한 번도 안 훑은 것으로 보인다** |
| M6 | 폼 검증이 제출 시에만 | [C] | 볼트가 **하이브리드**(입력 중 무검증 → blur 첫 검증 → 오류 후 실시간 → 제출 시 전체)를 답으로 제시하고 `격차-폼 검증 시점`을 이걸로 닫았다. HanQ는 서버 제출 시 1회뿐이고, **오류 시 첫 오류 필드로 스크롤+포커스도 없다**(`AskForm.tsx:65`는 하단 sticky 바에만 표시). 긴 폼에서 오류 문구가 화면 밖일 수 있다 |
| M7 | 다크 모드 미지원 + 미고정 | [C] | `prefers-color-scheme` 0건, `color-scheme` 선언도 0건. 볼트: **"지원하지 않을 거면 최소한 라이트 모드로 고정"** — 안 하면 일부 브라우저·OS가 강제 반전시켜 색이 깨진다. `themeColor: "#0064ff"`만 설정돼 있어 절반만 대응한 상태 |
| M8 | 삭제 후 포커스 유실 | [A] | `AnswerItem`의 `DeleteMenu`가 사라지면서 포커스가 `body`로. 볼트 8순간 중 **"삭제 후 → 인접 항목 또는 목록 컨테이너"** |
| M9 | 목록 하단에 도달할 수 없다 | [C] | 질문 50건 · 답변 100건 상한에 페이지네이션도 "더 보기"도 무한 스크롤도 없다. 볼트 비교표 기준 이 서비스에는 **Load More 또는 하이브리드**가 맞다. 지금은 **51번째 질문에 도달할 경로가 sitemap 말고 없다** |
| M10 | 입력 최적화 누락 | [C] | `autocomplete` 0건(프로필 이름 → `autocomplete="name"`) · `enterkeyhint` 0건(답변 입력 → `send`) · TextField 카운터에 **80% 주의색 없음** · textarea가 `resize-none`(볼트 권장은 `resize: vertical`) |
| M11 | 아바타 색 조합 대비 | [A] | green `#12b886` on `#e6f9f3` = **2.34:1** · red `#f04452` on `#feeced` = **3.26:1** · blue = 4.38:1 (purple만 7.27:1 통과). 이니셜 글자가 `aria-hidden`이라 정보 전달은 아니지만 **시각적으로 안 읽힌다** |
| M12 | FAB에 라벨이 없다 | [C] | `BottomNavigation.tsx:59` 질문 작성 버튼은 `aria-label`만 있고 시각 라벨이 없다. 볼트: **"아이콘 + 라벨 — 라벨 없는 아이콘은 인지율 하락"**. 다만 양옆 탭에는 라벨이 있어 대비로 읽히는 면도 있다 — 의도적 선택이면 그대로 둬도 된다 |

---

## 4. Low — 백로그 (4건)

| # | 항목 |
|---|---|
| L1 | `RelativeTime`이 갱신되지 않는다 — `subscribe`가 no-op(`RelativeTime.tsx:8`). "방금 전"이 화면에 계속 남는다 |
| L2 | `OpenInBrowser`의 `copied`가 복구되지 않아 버튼이 "복사됨"에 고정(`OpenInBrowser.tsx:26`) |
| L3 | `Categories`에서 전체 해제 시 URL 끝에 `?`만 남는다(`Categories.tsx:22`) |
| L4 | `Header`의 좌/우 슬롯이 `<div>` — 랜드마크 `<nav>`가 아니다. 헤더 자체는 `<header>`라 치명적이지 않다 |

---

## 5. 기준에 정확히 들어맞는 것

지적만 나열하면 무엇을 유지해야 하는지 알 수 없으므로 같이 적는다.

**볼트 규칙과 문자 그대로 일치하는 것 3개**

1. **상대 시간을 7일 이후 절대 날짜로 전환** — `format.ts:34~38`.
   볼트 UX 라이팅의 **"상대 시간은 7일 이후 날짜로 전환"** 과 정확히 같다.
2. **한국어 조사 문제를 회피하는 문장 구조** — `ko.ts:181~183`의 `${name}님이 답변을 남겼어요`.
   "님" 뒤는 조사가 항상 "이"로 고정되므로 받침 판별이 필요 없다.
   볼트가 최악의 예로 든 `홍길동님이(가)`를 **구조로 피해간 것** — 우연이라면 운이 좋고, 의도라면 정확하다.
3. **되돌릴 수 없는 작업에만 확인 모달** — 삭제는 모달, 좋아요·투표는 즉시 반영.
   볼트 흐름도의 결론(**"되돌릴 수 있으면 확인 모달 대신 Toast + Undo"**)과 같은 판단이다.
   Toast+Undo 쪽이 비어 있는 게 H8이지, 모달을 쓴 판단 자체는 맞다.

**그 밖에**

- **버튼 문구가 동사형** — "등록" "삭제하기" "돌아가기". 볼트가 금지한 "확인/취소" 쌍을 안 썼다
- **Skeleton이 실제 레이아웃 치수를 따라간다** — CLS가 거의 없다. 모든 라우트에 `loading.tsx`가 있고 `LoadingAnnouncement`로 스크린리더 안내까지 한다
- **낙관적 UI** — `LikeButton` · `PollVote` · `Tab` 셋 다 `useOptimistic`. 볼트가 `UI-상태`의 누락으로 지적한 기법을 이미 쓰고 있다
- **플레이스홀더를 라벨로 쓰지 않았다** — `TextField`는 `<label htmlFor>`, `AnswerInput`은 `sr-only` 라벨. 볼트: **"이 규칙 하나가 폼 접근성의 절반"**
- **탭을 radio로 구현** — `Tab.tsx:57`. 좌우 화살표 키 이동을 **브라우저에서 공짜로 얻는다.** 볼트가 "자주 틀리는 부분"으로 꼽은 지점을 우회한 셈
- **하단 탭 3개** — 3~5개 규칙 준수. 주요 액션이 엄지 존. `aria-current="page"` ✅
- **마찰을 뒤로 미뤘다** — 비로그인도 질문 상세를 읽을 수 있고, 답변하려 할 때 로그인을 요구한다. 볼트 온보딩의 **"가치 경험을 앞으로, 가입을 뒤로"** 와 같은 구조
- **다크 패턴 0건** — 14유형 대조 결과 해당 없음. 공지 배너에 닫기가 있고, 로그아웃 경로가 로그인만큼 짧다(대칭성 ✅)
- **탭·필터 전환 시 스크롤 위치 유지** — `scroll: false` (`Tab.tsx:42`, `Categories.tsx:22`)
- **빈 상태를 2유형으로 구분** — `emptyAll`(첫 사용) / `emptyFiltered`(결과 없음). 볼트가 "셋에 같은 화면을 쓰면 안 된다"고 한 구분을 절반 이상 지켰다
- **인앱 브라우저 대응** — `in-app-browser.ts`. 볼트에 아예 없는 항목인데 구현되어 있다(§7 참조)

---

## 6. 처리 순서 제안

볼트의 원칙 하나를 그대로 적용한다 —
**"같은 종류의 이슈가 반복되면 개별 수정이 아니라 디자인 시스템으로 해결한다."**

| 순서 | 작업 | 해결되는 항목 | 비용 |
|---|---|---|---|
| 1 | **색 토큰 4개 재산정** (`tertiary` `like` `disabled` `default`) | **H1 H2 H3 H4 M11** | `globals.css` 한 파일 |
| 2 | **`globals.css`에 전역 규칙 3블록** (`word-break`+`overflow-wrap` / `prefers-reduced-motion` / `color-scheme`) | **H5 H6 M7** | 한 파일 |
| 3 | **`env(safe-area-inset-bottom)` 3곳** | **H7** | 3줄 |
| 4 | `Header`의 title을 `<h1>`으로 + skip link | **H10** | 1컴포넌트 |
| 5 | Toast 컴포넌트 1개 신설 → 삭제·저장·투표 실패에 연결 | **H8** + `QA.md` 2-5 | 신규 |
| 6 | `DeleteMenu`에 focus trap + 복귀 + scroll lock | **H9 M8** | 1컴포넌트 |
| 7 | 제출 버튼 disabled 제거 → 클릭 시 부족한 필드로 포커스 | **H11 M6** | 2폼 |
| 8 | 일본어로 전 화면 1회 순회 | **M5** | 30분 |

**1~3번이 High 11건 중 7건을 처리하고, 건드리는 파일은 사실상 `globals.css` 하나다.**
색과 전역 CSS를 시스템 층에서 잡으면 그 토큰을 쓰는 모든 화면이 자동으로 따라온다 —
볼트가 접근성 페이지에서 한 말 그대로다.

---

## 7. 볼트에 되먹일 것

`other me`는 "적재 → 격차 발견 → 처방" 루프로 돌아가는 저장소다.
이번 QA에서 **볼트에 없어서 대조할 수 없었던 것**들을 격차 후보로 남긴다.

**기존 격차의 실사례 (해당 페이지에 붙일 것)**

- `격차-낙관적 UI 적용 기준` — HanQ `voteAction`이 **낙관적 UI를 쓰면서 실패를 안 알리는**
  정확한 사례다. "언제 쓰고 언제 쓰지 말아야 하는지"에 **"실패 경로가 설계돼 있을 때만 쓴다"** 를 채울 근거
- `격차-폼 검증 시점` — 하이브리드 미적용 실물 사례 (M6)
- `격차-디자인 QA 운영` — "체크리스트를 다 돌리면 화면당 얼마나 걸리나"에 대한 실측:
  **8화면 규모 서비스 · 코드 대조 방식 · 27개 기준 페이지 기준으로 약 1시간**

**새 격차 후보 3개**

1. **스트리밍/PPR 환경의 로딩 UX** — 볼트의 응답 시간 기준(Spinner ~1초, Skeleton 1~3초)은
   **요청 하나에 응답 하나**를 전제한다. HanQ는 정적 셸이 먼저 오고 본문이 나중에 흘러들어온다.
   "언제부터 스켈레톤을 보여줄 것인가"의 기준선 자체가 다르다.
   → 볼트의 `격차-응답 시간 임계값 정리`(이미 4번 충돌 중)에 **다섯 번째 축**으로 붙는다
2. **인앱 브라우저 UX** — 카카오톡·인스타그램 인앱 브라우저에서 소셜 로그인이 막히는 문제와
   외부 브라우저 유도 패턴. **한국 서비스에서는 필수인데 볼트 어디에도 없다.**
   HanQ는 `in-app-browser.ts`로 이미 구현해놨으니 **역방향 적재(코드 → 위키)** 가 가능하다
3. **번역 지연 상태의 UX** — 기계 번역이 비동기로 끝나는 서비스에서
   "번역 중" 중간 상태를 어떻게 보여주는가. 볼트의 `UI-상태` 데이터 상태 8종에 **Translating이 없다.**
   `Stale`("마지막 업데이트 5분 전")과 성격이 비슷한 자리다.
   HanQ의 `TranslatingBadge` + `translationPending`이 실물 사례

---

## 8. 검증 로그

### 대비비 실측 (WCAG 2.1/2.2 공식, 자체 계산)

```
토큰            vs #fff   vs muted  vs page   판정
ink #191f28     16.56     15.02     15.85    ✅ AA 본문
strong #333D4B  11.00      9.97     10.52    ✅ AA 본문
body #4E5968     7.11      6.45      6.81    ✅ AA 본문
brand #0064ff    4.92      4.46      4.71    ✅ AA 본문 (muted 위 4.46 경계)
secondary #6b7684 4.62      4.19      4.42    ✅ 흰 배경만 통과 / muted 위 미달
tertiary #8b95a1  3.04      2.76      2.91    ❌ 본문 미달  ← H1
like #f04452      3.71      3.37      3.55    ❌ 본문 미달  ← H2
default #E5E8EB   1.23      1.12      1.18    ❌ UI 3:1 미달 ← H4
muted #f2f4f6     1.10      1.00      1.06    ❌ 구분선
disabled #d1d6db  1.46      1.33      1.40    ❌ 플레이스홀더 ← H3

조합
#fff on brand   4.92  ✅ (버튼 15/17px)
#fff on like    3.71  ❌ (삭제 버튼 15px bold — 큰 글씨 기준 미달) ← H2
brand on brand-subtle 4.38  ❌ 경계 (공지 배너 13px)
아바타 blue 4.38 / purple 7.27 ✅ / red 3.26 ❌ / green 2.34 ❌  ← M11
```

### 터치 타깃 실측

```
✅ 44px 이상   Button md 44 · lg 56 · TextField 52 · AnswerInput 44 ·
              투표 선택지 48 · 모달 버튼 48 · FAB 52 · 하단 탭 링크(h-19 전체)
△ 24~44px    Chip 34 · Tab 라벨 40 · LikeButton md ≈36 · sm ≈30 · 공지 닫기 24
❌ 24px 미만  헤더 아이콘 버튼 20×20 (Bell · More · ChevronLeft · Close)
              → SC 2.5.8 Spacing 예외로 기술적 통과, 실무 권장 대비 크게 미달  ← M1
```

### 전역 CSS 커버리지

```
word-break / overflow-wrap ····· 0건  ❌ H5
prefers-reduced-motion ········· 0건  ❌ H6
env(safe-area-inset-*) ········· 0건  ❌ H7  (viewport-fit=cover는 켜짐)
prefers-color-scheme ··········· 0건  ⚠️ M7
color-scheme ··················· 0건  ⚠️ M7
skip link ······················ 0건  ❌ H10
Toast / Undo ··················· 0건  ❌ H8
autocomplete / enterkeyhint ···· 0건  ⚠️ M10
```

### 구조

```
h1        랜딩 ✅ · 상세 ✅ · 마이 ✅ · 유저 ✅
          홈 ❌ · 작성 ❌ · 알림 ❌ · 프로필수정 ❌   ← H10
alt 없는 <img> ··· 0건 ✅
landmark ········· <header> <main> <nav> 사용 ✅ (main은 페이지당 1개 유지)
aria-*  ·········· current="page" · pressed · expanded · modal · labelledby · alert · status 사용 ✅
타입 스케일 ······ 13단계 임의 px (권장 6~10 + 토큰)   ← M3
8pt 이탈 ········· [5px][6px][7px][10px][14px][18px][114px] 등   ← M4
```
